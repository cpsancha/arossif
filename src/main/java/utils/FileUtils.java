package utils;

import java.nio.file.Path;

public class FileUtils {
	
	public boolean checkIfExists(Path paht, boolean isDir, boolean createIfNot) {
		//TODO Write all the method contains
		
		return true;
	}
}
